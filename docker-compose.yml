version: '3'

services:
  postgres:
    container_name: postgres
    image: postgres:16.2-alpine3.19
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=studio26
    # База не нужна снаружи — лучше убрать проброс порта
    # ports:
    #   - 5432:5432
    volumes:
      - pg_data:/var/lib/postgresql/data

  backend:
    build: ./server
    restart: always
    environment:
      - PORT=3000
      - DB_NAME=postgres
      - DB_USER=postgres
      - DB_PASSWORD=studio26
      - DB_HOST=postgres
      - DB_PORT=5432
      - CLIENTURL=https://26studio.ru
    depends_on:
      - postgres
    # не публикуем наружу
    expose:
      - "3000"
    volumes:
      - my_app_data:/app/static
      - my_app_data:/app/extracted
      - my_app_data:/app/uploads

  frontend:
    build: ./client
    restart: always
    # не публикуем наружу
    expose:
      - "80"

  caddy:
    image: caddy:2.7
    restart: unless-stopped
    depends_on:
      - frontend
      - backend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  my_app_data:
  pg_data:
  caddy_data:
  caddy_config:
